import BuilderComponentProps from '../../types'

const cardGridBuilderProps: BuilderComponentProps = {
  name: 'threeCardRow',
  inputs: [
    {
      name: 'threeCardRowModel',
      type: 'reference',
      model: 'three-card-row'
    }
  ]
}

export default cardGridBuilderProps
